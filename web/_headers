# Cloudflare Pages 標頭設定
#
# COOP/COEP 會啟用 cross-origin isolation，這是 WebAssembly 多執行緒
# （SharedArrayBuffer）的前提。沒有這兩個標頭，onnxruntime-web 只能單緒跑，
# 在多核裝置上會慢好幾倍。
#
# 代價：跨來源的資源都必須明確允許被嵌入，否則會被擋掉。
# 所以模型權重如果放在別的網域（例如 R2 自訂網域），
# 那邊也要回 Cross-Origin-Resource-Policy: cross-origin。
/*
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Embedder-Policy: require-corp
  Cross-Origin-Resource-Policy: same-origin

# wasm 要用正確的 MIME 才能走 streaming 編譯（快很多）
/ort/*
  Content-Type-Options: nosniff
  Cache-Control: public, max-age=31536000, immutable

# 模型權重很大且不會變，讓瀏覽器與 CDN 盡量長期保存
/model/*
  Cache-Control: public, max-age=31536000, immutable
  Cross-Origin-Resource-Policy: cross-origin

# service worker 不能被快取，否則更新推不出去
/sw.js
  Cache-Control: no-cache
