{
  "name": "edge-cascade-llm-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "EdgeCascadeLLM 瀏覽器節點與裝置效能量測工具",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs && npx http-server dist -p 8080 -c-1 --cors",
    "test": "node --test test/*.test.mjs",
    "prepare-model": "node scripts/prepare-model.mjs"
  },
  "dependencies": {
    "onnxruntime-web": "^1.30.0"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
